//#region src/opencookies.ts
function toOpenCookiesConfig(policy, options) {
	const used = policy.cookies?.used ?? {};
	const categories = Object.keys(used).filter((key) => used[key]).map((key) => ({
		key,
		label: key.charAt(0).toUpperCase() + key.slice(1),
		locked: key === "essential"
	}));
	const policyVersion = options?.policyVersion ?? policy.cookieVersion;
	return {
		...options,
		...policyVersion ? { policyVersion } : {},
		categories
	};
}
//#endregion
export { toOpenCookiesConfig };

//# sourceMappingURL=opencookies.js.map